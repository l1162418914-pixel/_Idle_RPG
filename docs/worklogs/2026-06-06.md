# 工作日志 2026-06-06（计划）

## 元信息

| 项 | 值 |
|----|-----|
| 阶段 | **Sprint 重构** · T-REFACTOR-M1 |
| 定案 | [PROJECT_STATUS.md](../PROJECT_STATUS.md) §T-REFACTOR · [ARCHITECTURE.md](../ARCHITECTURE.md) §三-B |

## 明日第一步（用户定焦：出征→接战/返程战斗）

**环 1 地基**：先把 `PREPARE → RUNNING → 接战/返程战 → RESULT` 里 **战斗编排** 弄可信；大营 Meta / MIA **不插队**。

1. **T-REFACTOR-M1**（P0）— 见下「三场战斗分类验收」+ PROJECT_STATUS §M1-1～M1-5。
2. **并行可选**：T-MIA-0D（仅文档）。

### 三场战斗（明天逐场过）

| 模式 | `EncounterKind` | 推荐测试图 | 要弄清的行为 |
|------|-----------------|------------|--------------|
| 进军接战 | `MARCH_ADVANCE` / `BOSS_LANE` | test_01、进军 Boss 线 | 停滚距离；胜继续推进/败紧急撤 |
| 返程小怪战 | `MARCH_RETREAT` | test_01 返程、test_06 | 边撤边打；濒死后排；败不重复 begin_retreat |
| 首领追击接战 | `CHASE_BOSS` | **test_03** | 仅 1 Boss；无刷怪插队；胜结算/击退继续撤/败稳定罚 |

### 现网最可能还缺的钉（M1 对照改）

- `WorldRun.tick` 返程刷怪在 `chase_combat_in_progress` 时仍 **emit**（应在远征层暂停）
- 首领接战时 `distance_traveled` 仍 **在减**（应冻结或仅战场轴表现）
- 接战结束 `_pending_enemies` **链下一场**

## ~~暂停至 M1 YES~~（已解除 · M3 CTO YES 2026-06-06）

- T-MIA-0～4 代码 → **解禁**，下一开发 **T-MIA-0**
- T-02a / T-02e F5 → **并行 CTO 验收**
- T-RUN-V1 CTO 探针 → **并行 CTO 验收**

## M1 验收（F5）

- test_03：首领接战无小怪插队
- 击退/击杀无链战
- 首领接战距离冻结
- 进军停滚不退化

## 后续链

- M1 YES → **T-REFACTOR-M2**（SaveSerializer + GM 瘦身 + RunEventPresenter）
- M2 YES → **T-REFACTOR-M3**
- M1 YES + T-MIA-0D YES → T-MIA-0

---

## T-REFACTOR-M1 实现记录（开发 Agent）

**改动摘要：**
- `world_run.tick`：首领接战期间不 emit 返程 `enemy_spawn`、不推进 `distance_traveled`
- `RetreatSpawnService`：接战档位 `pack=0` 双保险
- `EncounterSession.mark_chase_combat_started` + 接战开始清 `_pending_enemies`；`_finish_combat` 统一清 pending
- `RunMarchLane`：返程首领接战冻结顶栏距离（返程小怪仍边撤边打）

**待 F5：** test_03 / test_01 / test_06 三场分类验收

**test_09 软锁修复：** 首领接战索敌优先可战队员；全队濒死（`get_combat_ready_count==0`）触发处决战败 → 灭团结算

---

## T-REFACTOR-M2 实现记录（开发 Agent）

- `SaveSerializer` / `MercRecruitService` / `MapUnlockService` / `InfirmaryService` 从 GM 迁出
- `RunEventPresenter` + `RunDriver` 从 `main.gd` 迁出；`main.gd` 薄壳委托
- 探针：`main.gd` < 500 · `game_manager.gd` < 900 · 存档字段不变

**冒烟修复（进军接战）**：`MARCH_ADVANCE` 下敌方不再锚点后撤；近战 ideal 位不回退（X 只减不增）

**冒烟（用户确认）**：test_01 过 — 怪物不再后撤；顶栏距离仅在战斗间隙推进（预期）

**test_03 修复**：追击僵持红底面板置顶战斗区 + 顶栏橙色横幅；修复 holding 被清零致蓄力 0%；接战结束/结算自动隐藏

**F5 验收（用户）**：test_01 过 · test_03 过（仅 1 Boss、有伤害、僵持击退 1 次）· test_09 追击灭团线验证 UI/击退

---

### [T-REFACTOR-M1] 接战门禁 + EncounterSession 收口

**完成内容：**
- M1-1：`chase_combat_in_progress` 期间不消费/不刷返程小怪；`EncounterSession.allows_pending_append` 收口
- M1-2：首领接战开始/结束清 `_pending_enemies`；`mark_chase_combat_started` 0.5s CD
- M1-3：首领接战冻结 `distance_traveled` / `RunMarchLane` 顶栏距离
- M1-4：胜败击退统一 `EncounterSession.resolve_*`；`RunDriver` 收口
- M1-5：`CombatController` 无 `boss_chase`/`is_chase_encounter` 字符串；移动走 `CombatMovementPolicy`
- 冒烟修复：进军接战敌方后撤；test_09 追击处决/索敌；追击僵持 UI + 蓄力 holding 修复

**影响文件：**
- `scripts/run/world_run.gd`
- `scripts/run/run_driver.gd`
- `scripts/run/encounter_session.gd`
- `scripts/run/retreat_spawn_service.gd`
- `scripts/combat/combat_controller.gd`
- `scripts/combat/combat_movement_policy.gd`
- `scripts/ui/run_march_lane.gd`
- `scripts/ui/combat_view.gd`
- `scripts/ui/run_ui.gd`
- `scripts/main.gd`

**测试步骤：**
1. test_01：进军接战怪物不前撤；顶栏距离单次接战内不增
2. test_03：追击仅 1 Boss；蓄力击退推远继续返程（结算见僵持击退次数）
3. `grep` `combat_controller.gd` 无 `boss_chase` / `is_chase_encounter`

**验收标准：**
- [x] test_03 首领接战全程无小怪插队
- [x] 击退后不秒再开战（僵持击退成功继续返程）
- [x] 首领接战顶栏距离冻结
- [x] 进军接战停滚不退化（test_01）
- [x] `CombatController` grep 探针通过

**是否允许进入下一任务：** YES（用户 2026-06-06 收 M1）

---

### [T-REFACTOR-M2] SaveSerializer + GM 瘦身 + RunDriver

**完成内容：**
- M2-1：`SaveSerializer` 承载 `to_save_dict` / `from_save_dict`；GM 委托
- M2-2：GM 瘦身 — `MercRecruitService` / `MapUnlockService` / `InfirmaryService` 迁出
- M2-3：`RunEventPresenter` 承接 `run_event` → UI 文案
- M2-4：`RunDriver` 承载出征 tick + 接战生命周期；`main.gd` 薄壳委托
- M2-5：序列化字段与 `SAVE_FORMAT` 一致，无删改

**影响文件：**
- `scripts/core/save_serializer.gd`（新）
- `scripts/core/game_manager.gd`
- `scripts/roster/merc_recruit_service.gd`（新）
- `scripts/run/map_unlock_service.gd`（新）
- `scripts/base/infirmary_service.gd`（新）
- `scripts/run/run_event_presenter.gd`（新）
- `scripts/run/run_driver.gd`（新）
- `scripts/run/test_scenario_service.gd`
- `scripts/main.gd`

**探针（静态）：**
- `main.gd` **177** 行（< 500）
- `game_manager.gd` **891** 行（< 900）
- `run_driver.gd` **665** 行

**测试步骤：**
1. F5 进大营 → 读档正常
2. 选图出征 → 接战/返程/结算流程与 M1 验收一致
3. 回大营再战 / 重启后存档仍在

**验收标准：**
- [x] SaveSerializer 迁出且 GM 委托
- [x] RunDriver + RunEventPresenter 迁出；main 无 run_event switch
- [x] 行数探针通过
- [x] F5 出征/结算（M1 冒烟 + test_09 实战路径已覆盖）

**是否允许进入下一任务：** YES（用户 2026-06-06 收 M2）

---

## T-REFACTOR-M3 实现记录（开发 Agent）

- M3-1：`WorldRunTickScheduler` + `AdvanceSpawnService` + `RetreatSpawnService.tick_spawns`；`world_run.tick` 遇敌改调度
- M3-2：`CombatProjectileSystem` + `CombatSkillExecutor` 从 `CombatController` 迁出
- M3-3：`main.gd` 移除 legacy UI 分支；`MainShell` 为唯一路径
- M3-4：`RunModeService.apply_for_departure` 出征 `run_mode` 桩（RECOVERY 锁 / NORMAL）

**待 F5：** test_01 + test_03 回归；T-02a/T-RUN-V1 探针可并行启动

---

### [T-REFACTOR-M3] WorldRun 调度化 + CombatController 拆分

**完成内容：**
- M3-1：`WorldRun.tick` 遇敌改 `WorldRunTickScheduler`；进军 `AdvanceSpawnService`、返程 `RetreatSpawnService.tick_spawns`
- M3-2：`CombatProjectileSystem` + `CombatSkillExecutor` 独立；Controller 委托
- M3-3：移除 `main.gd` legacy UI；`MainShell` 唯一路径
- M3-4：`RunModeService.apply_for_departure` 出征 `run_mode` 桩（RECOVERY/NORMAL）

**影响文件：**
- `scripts/run/world_run_tick_scheduler.gd`（新）
- `scripts/run/advance_spawn_service.gd`（新）
- `scripts/run/run_mode_service.gd`（新）
- `scripts/run/retreat_spawn_service.gd`
- `scripts/run/world_run.gd`
- `scripts/combat/combat_projectile_system.gd`（新）
- `scripts/combat/combat_skill_executor.gd`（新）
- `scripts/combat/combat_controller.gd`
- `scripts/core/game_manager.gd`
- `scripts/main.gd`

**测试步骤：**
1. test_01 — 进军/返程接战行为不退化
2. test_03 — 追击首领 + 僵持击退
3. F5 读档/出征/结算

**验收标准：**
- [x] `world_run.tick` 遇敌仅调度 *Service
- [x] 技能/投射物独立文件；Controller 无大块内联
- [x] `main.gd` 无 legacy 分支
- [x] `run_mode` 出征入口桩就位
- [x] F5 回归（用户收 M3）

**是否允许进入下一任务：** YES（用户 2026-06-06 收 M3）

---

### [T-MIA-0] 存档桩核对

**核对结论：**
- `SaveSerializer`：`account_meta` / `rescue_squad` 读写 + `normalize_*`；`is_mia` 序列化默认 `false` ✅
- `RunModeService`：`NORMAL` / `RECOVERY` 出征桩 ✅
- `WorldRun`：`RunMode` 枚举 + `settlement_tier` 字段 ✅
- `_apply_run_exp`：无 `settlement_tier` 分支 ✅

**缺口修复（1 处，属 T-MIA-2 前置误接）：**
- 移除灭团 → `settlement_tier=mia` 与 `enter_mia_state()`（`world_run.gd` / `run_driver.gd`）

**是否允许进入下一任务：** YES（用户收 T-MIA-0 · 探针核对通过；最小 diff 回退越界桩）

---

### [T-MIA-1] is_mia 数据层（核对）

**核对结论：**
- `Mercenary`：`is_mia` / `enter_mia_state` / `clear_mia_state` / `can_join_squad` 含 `not is_mia`；`mark_permanent_death` 清 MIA ✅
- `SaveSerializer`：`is_mia` 读写默认 `false` ✅
- `SquadFormationService.assign_merc_to_slot` → `-5`；`formation_error_message`「战场遗留，不可编入」✅
- `formation_ui` / `squad_ui` / `base_ui`：MIA 灰显 + 不可拖入出战槽 ✅

**是否允许进入下一任务：** YES（探针核对通过，无额外 diff）

---

### [T-MIA-2] 灭团改 MIA（第一刀）

**改动：**
- `run_driver.gd` `_mark_squad_wiped`：本趟上场成员 `enter_mia_state()`（`Player` 在 `enter_mia_state` 内跳过）；不调用 `mark_permanent_death`
- `world_run.gd` `_resolve_settlement_tier`：`squad_wiped` → `"mia"`
- `result_ui.gd`：已有「战场遗留」标题 + MIA 统计行（无 diff）

**探针（F5 + 截图）：**
- [x] 灭团后参战 merc 进「战场遗留」名册（非阵亡、非养伤）
- [x] 主角在「可出征名册」，不进 MIA
- [x] 灭团路径无 `mark_permanent_death`（代码核对）

**是否允许进入下一任务：** YES（用户 2026-06-06 收 T-MIA-2）

---

### [T-MIA-2+] 名册分区：MIA / 养伤

**改动：**
- `base_ui.gd`：右侧名册拆为「可出征 / 养伤 / 战场遗留」三节；医疗室统计排除 MIA
- `formation_ui.gd`：未编入池分可点选 / 养伤 / 遗留三组
- `squad_ui.gd`：准备页可用列表同规则分组

**是否允许进入下一任务：** YES（用户截图验收）

---

### [T-MIA-3] account_meta 冻结经验

**改动：**
- `game_manager.gd`：`settlement_tier=mia` → `_record_frozen_exp_pool`（B-6 比例）；`_apply_run_exp` 跳过；`success`/`manual` 不变
- `end_run` 预写 `frozen_exp_recorded` 供结算 UI
- `result_ui.gd`：MIA 结算显示冻结经验文案

**补丁（测试图）：**
- `test_run_ephemeral`：结算页正常展示金币/经验/掉落/冻结；**回大营不入账**
- 出征前快照名册/稳定度/编队，回城还原正式进度
- `is_test_stand_in`：任务佣兵锁定（免疫状态 + UI 不进养伤/遗留/阵亡）

**探针（待 F5）：**
- [ ] 正式图灭团回城：`frozen_exp_pools` 有记录
- [ ] 测试图灭团：池 **不**增；回城后战场遗留清空
- [ ] 正常抵营成功：池不增、经验仍入账
- [ ] 手动斩仓：无 MIA、经验按现网入账

**是否允许进入下一任务：** 待用户验收

**补丁（测试图 Boss 攻击）：**
- `combat_entity.gd`：测试佣兵战斗内可正常扣血/显示伤害（HP 不低于 1，不倒地/阵亡）
- `combat_controller.gd`：`sync_allies_hp_to_mercs` 测试佣兵回写满血

**补丁（测试⑨ 濒死/灭团）：**
- `TestScenarioService.allows_run_casualties`：`long_chase_pressure` 等图放开任务佣兵濒死/MIA
- 伤亡免疫改为 `test_merc_blocks_casualties`（仅非伤亡模拟图锁定）；回城 ephemeral 仍重置

**补丁（倒地/阵亡不跟队撤离）：**
- 返程接战不 `try_clear_near_death`；**阵亡**实体不 `_drift_homeward`；**濒死**随队慢速后撤、不攻击
- 伤亡模拟图任务佣兵 `init_from_merc` 用真实 HP；全队阵亡时返程立即灭团

**补丁（封存守卫战卡死）：**
- `_finish_combat` 先清上一场 UI，再开守卫战（避免 `prepare_between_encounters` 断开新战斗信号）
- 战斗 `inactive` 时自动收尾；强制撤离时正确 `abort_active_combat`

---

### [T-MIA-4] 主城回收 UI 占位

**改动：**
- `recovery_ui.gd`：后勤 **回收** Tab — MIA 列表、放弃搜寻（二次确认）、大价值复活占位
- `game_manager.gd`：`abandon_mia_search` / `_prune_frozen_exp_for_merc`；冻结池写入 `member_ids`
- 名册/编组/准备页统一 **`[遗留]`** 标签；结算指引改「后勤 F5 · 回收」

**探针（待 F5）：**
- [ ] 大营 F5 → 回收 Tab 可见 MIA 队员
- [ ] 放弃搜寻 → 永久阵亡、移出遗留、冻结池扣减
- [ ] 大价值复活 → toast「待接 T-MIA-P2」
- [ ] 编组池 `[遗留]` 不可编入

**是否允许进入下一任务：** 待用户验收

---

### [T-MIA-P2] 回收出征 RECOVERY 模式

**改动：**
- `recovery_run_service.gd`：短程（默认 72m）、禁小怪/Boss 追击、敌属性 ×0.55
- `game_manager.gd`：`start_recovery_run`、25% 冻结解冻入账、清 MIA；`recovery_run_target_ids`
- `world_run.gd` / `run_driver.gd` / `advance_spawn_service.gd`：抵点即胜、回收失败濒死（不新增遗留）
- `recovery_ui.gd`：大价值复活 → 发起回收出征
- `result_ui.gd`：`recovery` / `recovery_fail` 结算文案
- `mia_phase1_probe.gd`：P2a/P2b 逻辑探针

**探针（待与 Phase 1 R1～R7 一并 F5）：**
- [ ] 大营 F5 → 回收 → 大价值复活 → 进入短程 Run，里程约 72m
- [ ] 抵点 → 结算「回收成功」；目标队员清 `[遗留]`；冻结池扣减并解冻 25% 经验
- [ ] 回收途中灭团 → 「回收失败」；捞人队濒死、无新 MIA
- [ ] headless：`tools/MiaPhase1Probe.tscn`（含 P2a/P2b）

**是否允许进入下一任务：** 待用户与 Phase 1 一并验收

---

### [测试⑨] 死战灭团图（MIA / 回收手测）

**改动：**
- `test_09_long_chase_pressure` → 场景 `mia_wipe`：90m Boss 线、双人 L14 破损装、无追击、Boss ×5.2
- 选图注入冻结池 1200（`grassland`）；灭团回城 **保留** `[遗留]`（非普通测试图重置）
- `test_scenario_service.gd` / `game_manager.gd` 配套

**手测：** 选⑨ → 抵 Boss 灭团 → 回大营 F5 回收 → 大价值复活短程回收

**补丁（F5 回收为空）：**
- `end_run` 时 `finalize_mia_wipe_after_run` 锁定遗留并清 `_test_run_baseline`（不再 `ensure_test_run_session` 重抓学徒快照）
- `return_to_base`：`should_skip_test_session_restore` 时跳过 `_finish_test_run_session`；有遗留时跳过 `_sanitize_roster_for_base`
- `_sanitize_roster_for_base`：测试佣兵 `is_mia` 不再被回满血清状态
- `finalize` 用 `_force_mia_on_roster_merc` 绕过战斗内伤亡锁
- 探针增 **P2f**（`return_to_base` 后遗留仍≥2）

**补丁（选图即注入编队）：**
- `sync_roster_for_map_selection`：F4 点选测试⑨即注入 t09（不必先点卡片「出征」才看见编队）
- `base_ui` 选图 toast + `_refresh` 回城后自动同步；编组页更新测试⑨指引文案

**补丁（放弃搜寻无效）：**
- `mark_permanent_death` 对 `is_test_stand_in` 原为空操作 → `abandon_mia_search` 改为移出名册
- `recovery_ui` 确认框 `%s` 格式化修复
- `base_ui` 测试佣兵 `is_mia` 归入「战场遗留」侧栏

**补丁（入口三槽位 + 遗留不进替补）：**
- `CharacterCreate`：槽位 1~3 列表（继续 / 新建 / 删除 / 覆盖）；启动不再偷读档
- `SaveManager.peek_slot_summary`；测试档显示 `[测试档 fixture]`
- `SquadFormationService`：`is_mia` / 阵亡移出 A/B 槽；`rebalance`/`auto_fill` 不再塞替补
- 探针 **P2h**（遗留不进编队槽）

**补丁（回收按钮语义对调）：**
- `recovery_ui.gd`：新增 **发起回收** → `start_recovery_run`（短程跑图）；**大价值复活** → `try_high_value_mia_revive`（大营即时，不跑图）
- `game_manager.gd`：`get_high_value_mia_revive_cost`（普通复活价 ×12）、`try_high_value_mia_revive` 扣金后 `_apply_recovery_settlement`；`last_high_value_revive_summary` 供 toast
- `mia_phase1_probe.gd`：**P2i** 大价值复活不跑图、扣金、解冻

**探针（待 F5）：**
- [ ] F5 → **发起回收** → 进入短程 Run ~72m，抵点结算
- [ ] F5 → **大价值复活** → 留在大营 toast，清遗留、解冻 25%，扣金币
- [ ] F2 编组：遗留仅在「战场遗留」区，替1/替2 无 fixture
- [ ] F5 放弃搜寻 → 该行消失、冻结池扣减
- [ ] 选测试⑨ → 编组 A 应见 ⑨·前排 / ⑨·术士（非学徒）
- [ ] 灭团结算标题「战场遗留」→ 返回基地 → F5 回收见 t09×2 + 冻结经验
- [ ] 编组/名册见 `[遗留]`，勿再点地图「出征」（toast 拦截）
- [ ] headless：`MiaPhase1Probe.tscn` P2e/P2f

---

## CTO 验收（2026-06-06）

| TASK | 结论 | 备注 |
|------|------|------|
| T-REFACTOR-M1 | **YES** | test_01 / test_03 / test_09；编排可测 |
| T-REFACTOR-M2 | **YES** | SaveSerializer + RunDriver；行数探针 |
| T-REFACTOR-M3 | **YES** | 调度化 + Combat 拆分 + legacy UI 移除 + RunMode 桩 |
| T-MIA-0D | **YES** | docs `e7f40b6` |

**环 1 重构收口。** `PROJECT_STATUS` 已更新：当前指针 **T-MIA-0**；T-02a / T-RUN-V1 解禁待验。

---

### [T-MIA-P2] 回收按钮语义对调 + 大价值即时复活

**完成内容：**
- 普通回收「发起回收」→ `start_recovery_run`（短程跑图 ~72m）
- 大价值「大价值复活」→ `try_high_value_mia_revive`（大营即时，扣金 ×12，不创建 Run）
- toast 显示扣金与解冻经验；探针 P2i

**影响文件：**
- `scripts/ui/recovery_ui.gd`
- `scripts/core/game_manager.gd`
- `tools/mia_phase1_probe.gd`
- `docs/worklogs/2026-06-06.md`

**测试步骤：**
1. `python tools/build_test_save.py` → 游戏 **继续游戏**（槽位 1）
2. F5 回收 → 点 **大价值复活** → 留大营、toast 含扣金/解冻、该行消失
3. F5 → 对另一遗留点 **发起回收** → 进入短程 Run，抵点「回收成功」
4. （可选）`godot --headless --path <根> --scene res://tools/MiaPhase1Probe.tscn` 含 P2i

**验收标准：**
- [ ] 大价值复活不跑图、清遗留、扣金币
- [ ] 发起回收进入短程 Run
- [ ] F2 遗留不进替补席
- [ ] 放弃搜寻有效

**是否允许进入下一任务：** YES（待 CTO F5 复核）

---

### [T-MIA-0] 存档桩缺口核对

**完成内容：**
- 核对 0-1～0-4：现网已覆盖；`SAVE_FORMAT.md` 补全 `frozen_exp_pools` 的 `member_ids` / `mia_count` / `field_count` / `timestamp`
- 新增 `tools/mia_phase0_probe.gd` + `MiaPhase0Probe.tscn`（四则探针）

**影响文件：**
- `docs/SAVE_FORMAT.md`
- `tools/mia_phase0_probe.gd`
- `tools/MiaPhase0Probe.tscn`
- `docs/worklogs/2026-06-06.md`

**测试步骤：**
1. `godot --headless --path <根> --scene res://tools/MiaPhase0Probe.tscn`

**验收标准：**
- [x] 新档含 `account_meta` / `rescue_squad`
- [x] 旧档缺键 normalize 不崩
- [x] `RunMode.NORMAL` 默认
- [x] `SAVE_FORMAT` 与序列化一致

**是否允许进入下一任务：** YES（待 CTO 复核）

---

### [T-MIA-P3] 濒死二段 · 第一刀（护盾 / 压力 / 回收濒死）

**完成内容：**
- `near_death_config.downed_shield`：濒死授予护盾、压力锁 1
- 压力清零（出征中）→ 濒死；护盾击破 → 单体 `enter_mia_state`（非灭团）
- 回收成功改 `apply_near_death_state(0.4)`（§5.5 濒死+伤痕，非回满）
- 探针 P3a～P3d；P2a 同步验收回收后仍濒死

**影响文件：**
- `data/near_death_config.json`
- `scripts/mercenary/mercenary.gd`
- `scripts/combat/combat_entity.gd`
- `scripts/combat/combat_controller.gd`
- `scripts/core/game_manager.gd`
- `scripts/run/squad.gd`
- `tools/mia_phase1_probe.gd`
- `docs/worklogs/2026-06-06.md`

**测试步骤：**
1. headless：`MiaPhase1Probe.tscn`（含 P3a～P3d）
2. F5 大价值/跑图回收 → 队员回营应为 **濒死** 非满血
3. 出征中（后续手测）：濒死挨揍至护盾破 → 单体遗留（待 F5）

**验收标准：**
- [x] 逻辑探针 P3a～P3d
- [ ] F5 回收后濒死状态可见
- [ ] F5 战中二段死亡→遗留（待手测）

**是否允许进入下一任务：** YES（P3 逻辑探针齐；F5 手测可并行 CTO 验收）

**补丁（撤离失败 B-3a/b）：**
- `retreat_failure_mia_service.gd`：未抵营按濒死/压力档分流 MIA
- `run_driver._mark_squad_wiped`：返程中灭团 → `retreat_failure`（非 B-1 即时全员 MIA）
- `game_manager.end_run`、结算文案；探针 P3e/P3f/P3g

**补丁（压力收场 / 撤离事件）：**
- `pressure_outcome_service.gd`：B-3d 轻判定 → `pressure` 撤离事件；B-3c 单人替补换人
- 团队稳触顶不再 `forced` 裸返程；`retreat_reason=pressure`；抵营走 B-3e 养伤
- 探针 P3h/P3i

**补丁（主角强制回城 B-3h/i）：**
- `player_forced_return_service.gd`：濒死护盾破→主角离阵回城；永不 MIA
- `combat_entity` / `run_driver`：佣兵可留场；独阵触发紧急撤离
- `world_run.end_run`：`player_alive` 改 `squad.is_player_alive()`；`player_forced_return` 标志
- `run_ui` 回城 overlay；`result_ui`「指挥官独自回城」
- 探针 P3j/P3k/P3l

**补丁（B-11 恶化计趟）：**
- `mia_deterioration_service.gd`：过补给点 + 未捞 → `skipped_runs++`；2 趟 `map_point_visible=false`
- `world_run`：`supply_point_passed` / `max_distance_reached`；草原 `supply_distance:90`
- 回收衰减解冻比；地图回收 `-6`；大价值复活仍可用
- 探针 P3m/P3n/P3o

**补丁（B-3c 二阶段 + 换人读条）：**
- `pressure_outcome_service.apply_camp_pressure_settlement`：抵营 quota→加权 roll MIA
- `combat_controller`：`eject_pressure_substitute` / `deploy_pressure_substitute`；run_driver 读条延迟
- `run_ui` 3→2→3 overlay；探针 P3p/P3q

### [T-MIA-P4] 救援队 + 停尸间

**完成内容：**
- `rescue_squad_service` / `rescue_run_service` / `morgue_service`
- `WorldRun.RunMode.RESCUE`：避战抵点、失败养伤 CD（B-12f）
- 成功：运尸入 `morgue_queue`、清 MIA/冻结池、救援声望/等级
- `recovery_ui`：救援队按钮 + 停尸间医疗复活
- 探针 P4a～P4e

**影响文件：**
- `scripts/run/rescue_*.gd`、`morgue_service.gd`
- `scripts/core/game_manager.gd`、`save_serializer.gd`
- `scripts/mercenary/mercenary.gd`、`world_run.gd`、`run_driver.gd`
- `scripts/ui/recovery_ui.gd`、`result_ui.gd`
- `data/near_death_config.json`、`tools/mia_phase1_probe.gd`

**测试步骤：**
1. headless：`MiaPhase1Probe.tscn`（含 P4a～P4e）
2. F5 后勤：遗留行「救援队」→ 抵点 → 停尸间「医疗复活」

**验收标准：**
- [x] 逻辑探针 P4a～P4e
- [ ] F5 救援队出征 + 停尸间链（待手测）

**是否允许进入下一任务：** YES（MIA P2～P4 逻辑齐；并行 CTO 验收 / F5）

### [T-MIA-P5] 回城卷轴 + 读条一键（B-7 / §5.2）

**完成内容：**
- `return_scroll_service.gd`：回收失败发卷轴（绑批）；救援队失败不发
- `instant_recovery_service.gd` + `try_instant_mia_recovery`：读条一键、卷轴减价
- `recovery_ui`：读条 overlay +「读条一键」按钮；遗留行显示卷轴数
- `account_meta.return_scrolls`；探针 P5a～P5e

**是否允许进入下一任务：** YES（MIA 回收五通道逻辑齐：短Run/读条/大价值/救援/放弃）

### [B-10] 双半组 A/B 互捞

**完成内容：**
- `mutual_recovery_service.gd`：MIA 结算记 `mia_last_deploy_half`；对半组 `pick_target` / `describe_pending`
- `game_manager.start_run(skip)`：未跳过时自动 `_begin_recovery_run`（`mutual_recovery_this_run`）；`end_run` 写 `result.mutual_recovery`
- `save_serializer`：`mutual_recovery_auto`、`mia_last_deploy_half`
- `squad_ui`：互捞提示 +「本趟正常远征（跳过互捞）」；出发按钮「出发·互捞」
- `result_ui`：互捞成功标题「互捞回收成功」
- 探针 P6a～P6e；顺带修探针 `NormalMercenary` / `WorldRun.new` 桩

**影响文件：**
- `scripts/run/mutual_recovery_service.gd`
- `scripts/core/game_manager.gd`、`save_serializer.gd`
- `scripts/ui/squad_ui.gd`、`result_ui.gd`
- `data/near_death_config.json`
- `tools/mia_phase1_probe.gd`

**测试步骤：**
1. headless：`godot --headless --path <根> --scene res://tools/MiaPhase1Probe.tscn`（P6a～P6e 全 PASS）
2. F5：A 半组 MIA 后 B 半组出征 → 默认短程回收；勾选跳过 → 正常远征

**验收标准：**
- [x] P6a MIA 结算记录半组
- [x] P6b 对半组互捞目标
- [x] P6c `start_run` 自动 RECOVERY
- [x] P6d B-10a 跳过互捞
- [x] P6e 关闭互捞不劫持
- [ ] F5 编队互捞 UI（待手测）

**是否允许进入下一任务：** YES（B-10 逻辑探针齐；F5 手测可并行 CTO 验收）

### [B-12-unlock] 救援站建筑局外解锁

**完成内容：**
- `base_data.json` 新增「救援站」`rescue_station`（Lv.1 解锁救援队）
- `MorgueService.is_rescue_unlocked` 改判建筑等级；`sync` / `migrate_legacy_rescue_unlock` 兼容旧档
- `upgrade_building` 同步 `account_meta.rescue_unlocked`；新档默认未解锁
- `recovery_ui`：未解锁时禁用「救援队」并提示升级救援站
- 探针 P7a～P7b

**测试步骤：**
1. headless：`MiaPhase1Probe.tscn`（P7a～P7b PASS）
2. F5：新档后勤「救援队」灰显 → 大营升级救援站 Lv.1 → 按钮可用

**是否允许进入下一任务：** YES（MIA P2～P5 + B-10 + 救援解锁逻辑齐）

### [T-RUN-V2] 视差 + 行军队列（§八～§九）

**完成内容：**
- `parallax_backdrop.gd`：2～3 层视差，只读 `scroll_x` / 进军·返程方向
- `run_march_view.gd`：ColorRect 队伍剪影，进军/返程朝向切换
- `run_march_lane.gd`：集成视差 + 行军层；`_refresh_visuals()`；最小高度 48px；`on_run_started(run, party_count)`
- `main_shell.gd`：底栏行军宿主最小高度 22→52
- `run_driver.gd`：出征时传入 `party_count`
- `run_march_lane` 用 `preload` 引用视差/行军脚本，避免 headless 未 import 时 `class_name` 解析失败
- 探针 V2a/V2b

**影响文件：**
- `scripts/ui/parallax_backdrop.gd`（新）
- `scripts/ui/run_march_view.gd`（新）
- `scripts/ui/run_march_lane.gd`
- `scripts/ui/main_shell.gd`
- `scripts/run/run_driver.gd`
- `tools/mia_phase1_probe.gd`

**测试步骤：**
1. headless：`godot --headless --import --quit-after` 后 `MiaPhase1Probe.tscn`（V2a/V2b PASS）
2. F5 正式/测试图出征：底栏可见多层视差滚动 + 队伍剪影；返程向左；BASE 待机仍为篝火占位

**验收标准：**
- [x] V2a 进军 `MarchAdvance` + 视差/行军层可见
- [x] V2b 返程 `MarchRetreat` + 行军层可见
- [ ] F5 肉眼确认视差速度与接战停滚（待手测）

**是否允许进入下一任务：** YES（逻辑探针齐；F5 视差手测可并行 CTO）

### [探针套件清零] MiaPhase1Probe 全绿

**完成内容：**
- 探针佣兵模板 `apprentice` → `warrior_normal`（修复 max_hp=1 导致回收后濒死被 `try_clear_near_death_for_deploy` 误清）
- P2g：放弃搜寻前对 `t09_mage` 同步 `enter_mia_state`
- `_ensure_probe_player` / `_make_probe_normal` / P6 半组桩等既有修补保留
- 全量 headless：**50 PASS / 0 FAIL**，exit 0

**影响文件：**
- `tools/mia_phase1_probe.gd`
- `scripts/ui/run_march_lane.gd`（preload 编译修复）

**测试步骤：**
```powershell
$Godot = "<Godot_v4.6.3_console.exe>"
Set-Location "<项目根>"
& $Godot --headless --import --quit-after
& $Godot --headless --path . --scene res://tools/MiaPhase1Probe.tscn
```
期望：`ALL LOGIC PROBES PASSED`，进程 exit 0。

**验收标准：**
- [x] R1～R7、P2～P7、V2a/V2b 全 PASS
- [x] 无 FAIL 行、exit code 0

**是否允许进入下一任务：** YES

### [T-MIA-0] 存档桩核对（快检 + 补洞）

**完成内容：**
- 核对 M3 已交付项：`SaveSerializer` 读写 `account_meta` / `rescue_squad`；`WorldRun.RunMode`；`settlement_tier` 桩
- 四则探针 **0-2a～0-4** 全 PASS；增补 **0-2c** `is_mia` 序列化往返
- `SAVE_FORMAT.md` 同步：`account_meta` 扩展字段、`settlement_tier` 全档、`RunMode` 运行时说明
- **无玩法代码改动**（缺口仅在文档漂移）

**影响文件：**
- `docs/SAVE_FORMAT.md`
- `tools/mia_phase0_probe.gd`

**测试步骤：**
```powershell
& $Godot --headless --path . --scene res://tools/MiaPhase0Probe.tscn
```
期望：5 PASS · `ALL T-MIA-0 PROBES PASSED` · exit 0。

**验收标准：**
- [x] 新档 `to_save_dict` 含 `account_meta`、`rescue_squad`
- [x] 旧档缺键读档不崩 + normalize 默认
- [x] `WorldRun.run_mode` 默认 `NORMAL`
- [x] `settlement_tier` 桩 success/mia/manual/recovery（+ rescue 系运行时）
- [x] `SAVE_FORMAT` 与 `SaveSerializer` 字段一致

**是否允许进入下一任务：** YES（可开 T-MIA-1 门禁；现网 MIA 玩法已超前实现，建议 CTO 同步任务板状态）

### [T-RUN-V3] 接战锚点 + 敌从右入画

**完成内容：**
- `BattlefieldSlots`：`distance_to_anchor_shift`、`ally/enemy_position_at_slot`、`ENEMY_ENTRY_OFFSET_RIGHT`
- `CombatController.init_combat` 接收 `party_anchor_distance`；友敌 spawn 相对锚点平移
- `run_driver`：先 `on_combat_start` 冻结 lane，再传入 `party_anchor_x` 初始化战斗
- 濒死后排归位随锚点偏移
- 探针 V3a/V3b

**影响文件：**
- `scripts/combat/battlefield_slots.gd`
- `scripts/combat/combat_controller.gd`
- `scripts/run/run_driver.gd`
- `scripts/ui/combat_view.gd`
- `tools/mia_phase1_probe.gd`

**测试步骤：**
1. headless：`MiaPhase1Probe.tscn`（V3a/V3b PASS，全 52 PASS）
2. F5 test_01：进军接战敌群应在友方右侧；深程接战站位更靠右

**验收标准：**
- [x] V3a 锚点随里程右移、敌在友右
- [x] V3b 进军接战冻结 `party_anchor_x` / `scroll_x`
- [ ] F5 接战站位肉眼验收

**是否允许进入下一任务：** YES（下一项 T-RUN-V4 返程战背景同向）

### [T-RUN-V4] 返程行军与返程战背景同向

**完成内容：**
- `run_march_lane`：仅进军接战 `freeze_distance`；返程接战视差慢速左滚（×0.55）
- `parallax_backdrop`：修正进军/返程滚动方向；支持 `speed_mult`；`first_layer_offset_x` 供探针
- `combat_view`：返程接战战场 lane 随 `scroll_x` 左移（与 `_drift_homeward` 同向）
- `run_driver`：返程接战绑定 scroll 锚点；每 tick 同步战场位移
- 探针 V4a～V4c

**影响文件：**
- `scripts/ui/run_march_lane.gd`
- `scripts/ui/parallax_backdrop.gd`
- `scripts/ui/combat_view.gd`
- `scripts/run/run_driver.gd`
- `tools/mia_phase1_probe.gd`

**测试步骤：**
1. headless：`MiaPhase1Probe.tscn`（V4a～V4c PASS，全 55 PASS）
2. F5 test_01 返程：非战斗向左跑；返程接战背景仍向左、距离继续减

**验收标准：**
- [x] V4a 返程接战不 freeze、scroll 随 world tick 减少
- [x] V4b 进军接战 freeze scroll/视差
- [x] V4c 返程里程减时视差左移
- [ ] F5 返程战肉眼同向（待手测）

**是否允许进入下一任务：** YES（下一项 T-RUN-V5 Boss 追击剪影）

### [T-RUN-V5] Boss 追击剪影 + 接战切换抛光

**完成内容：**
- 新增 `BossChaseSilhouette`：返程 Boss 追击激活且非接战时，底栏右侧显示逼近剪影（位置/色调随 `get_boss_chase_gap()` 变化）
- `RunMarchLane` 集成剪影；`on_world_tick` 同步 chase 快照；接战时隐藏剪影
- 接战结束 **0.3s** 延迟再恢复行军（`RunMarchView` 在延迟期间保持隐藏，`lane_state` 待计时结束后切回 `MarchAdvance`/`MarchRetreat`）
- 探针 V5a/V5b

**影响文件：**
- `scripts/ui/boss_chase_silhouette.gd`（新建）
- `scripts/ui/run_march_lane.gd`
- `tools/mia_phase1_probe.gd`

**测试步骤：**
1. headless：`godot --headless --import --quit-after` 后 `MiaPhase1Probe.tscn`（V5a/V5b PASS，全 **57 PASS**）
2. F5 `test_03_boss_chase`：返程 Boss 追击时底栏右侧见红色剪影逼近；接战胜后约 0.3s 恢复向左行军

**验收标准：**
- [x] V5a 追击剪影可见、gap 缩小时更靠左、接战时隐藏
- [x] V5b 接战结束进入 0.3s resume 延迟，结束后恢复行军视图
- [ ] F5 test_03 肉眼验收

**是否允许进入下一任务：** YES（T-RUN 视差专题 V1～V5 逻辑层收工；待 CTO 复核 F5）

---

### [CTO] PROJECT_STATUS 同步（T-RUN-V + MIA 板）

**更新摘要：**
- **T-RUN-V1～V5** → ✅ CTO YES（57 PASS；F5 `test_01`/`test_03` 肉眼待手测）
- **T-MIA-0～2** → ✅ CTO YES
- **T-MIA-3/4、P2～P5** → 🟡 逻辑齐 · 并行 F5
- **当前开发指针** → **T-UI-B1.5**

### [T-UI-B1.5] Dock + 地图鼠标优先

**完成内容：**
- 收口 B1.5-1～5 现网实现：地图卡选中/出征分离、Dock 地图/编组滚动+描边、后勤 Tab、PREPARE 两行+展开
- 后勤瘦身：移除 Dock「再战」与 `base_ui`「再战上次」；RESULT 保留「同地图再战」
- Dock「编组」：BASE 编组区 pulse；PREPARE 中窗 pulse
- 探针 B1.5a/B1.5b（59 PASS 总量）

**影响文件：**
- `scripts/ui/main_shell.gd`
- `scripts/ui/base_ui.gd`
- `scripts/ui/formation_ui.gd`
- `scripts/ui/squad_ui.gd`
- `scripts/ui/result_ui.gd`
- `tools/mia_phase1_probe.gd`

**测试步骤：**
1. headless：`MiaPhase1Probe.tscn`（B1.5a/B1.5b PASS，全 **59 PASS**）
2. F5 **零键盘**：test_01 点卡选中→出征→出发→回营（仅用鼠标）

**验收标准：**
- [x] B1.5a 选中/出征分离 + 出征钮≥36px
- [x] B1.5b Dock≥36px 契约 + 无再战重复
- [ ] F5 零键盘全流程（待手测）
- [ ] Dock 地图/编组滚动描边肉眼（待手测）

**是否允许进入下一任务：** YES（逻辑/契约探针齐；F5 手测可并行 CTO 验收）

---

### [CTO] PROJECT_STATUS 同步（T-UI-B1.5）

- **T-UI-B1 / B1.5** → ✅ CTO YES（用户收）
- **当前开发指针** → **T-UI-B2**
- 探针总量 **59 PASS**

### [T-UI-B2] 顶栏稳定度 / 养伤锁上移

**完成内容：**
- 顶栏「稳定」进度条 + 数值：BASE 回城恢复 / PREPARE 出征预览 / RUNNING 团队+个人最低（含压力倍率）
- 养伤锁文案上移顶栏 `_top_recovery`，含 `get_recovery_lock_message` + ETA 秒数
- `run_driver` 每 tick 推送 `apply_run_snapshot`；底栏 `RunUI` 隐藏 `stability_label`
- `base_ui` 金币行移除重复稳定度文案
- 探针 B2a/B2b

**影响文件：**
- `scripts/ui/main_shell.gd`
- `scripts/ui/run_ui.gd`
- `scripts/ui/base_ui.gd`
- `scripts/run/run_driver.gd`
- `tools/mia_phase1_probe.gd`

**测试步骤：**
1. headless：`MiaPhase1Probe.tscn`（B2a/B2b PASS，全 **61 PASS**）
2. F5：大营顶栏见稳定条；养伤锁时顶栏橙字+预计秒数；RUNNING 顶栏显示团队/个人稳定，底栏无重复稳定行

**验收标准：**
- [x] B2a 顶栏稳定条 + run 快照 + 养伤锁服务文案
- [x] B2b 底栏稳定度迁出
- [ ] F5 顶栏/底栏肉眼验收

**是否允许进入下一任务：** YES（契约探针齐；F5 手测可并行 CTO 验收）

---

### [CTO] PROJECT_STATUS 同步（T-UI-B2）

- **T-UI-B2** → ✅ CTO YES（用户收）
- **当前开发指针** → **T-UI-B3**
- 探针总量 **61 PASS**

### [T-UI-B3] 中窗编组视觉

**完成内容：**
- 新增 `FormationSlotCard`：左色条 + 名/等级 + HP 条 + 状态徽章（可出战/休整/濒死/遗留），槽高 40px，保留拖拽/右键/点击换位
- `formation_ui`：A/B 半组包入舞台 `PanelContainer`（优先半组描边）；主角卡面板样式；养伤锁区文案指向顶栏
- 探针 B3a/B3b

**影响文件：**
- `scripts/ui/formation_slot_card.gd`（新建）
- `scripts/ui/formation_ui.gd`
- `tools/mia_phase1_probe.gd`

**测试步骤：**
1. headless：`MiaPhase1Probe.tscn`（B3a/B3b PASS，全 **63 PASS**）
2. F5 大营：中窗见 A/B 双舞台卡；出战/替补为卡牌+血条；空槽可拖入

**验收标准：**
- [x] B3a 槽卡 ≥36px + HP 条
- [x] B3b 半组舞台 + FormationSlotCard 接入
- [ ] F5 编组卡牌肉眼验收

**是否允许进入下一任务：** YES（探针齐；F5 手测可并行 CTO 验收）

### [T-UI-B4] 右窗大营背包网格

**完成内容：**
- 新增 `BaseCampBagUI`：6 列只读网格预览（`CELL_PX=14`、紫灰配色），摘要行 + 溢出提示 +「管理装备」按钮（36px）
- `base_ui` 右窗 `BagPlaceholder` 替换为 `CampBagScroll` + `_camp_bag_ui`；`_refresh()` 同步背包格
- 探针 B4a/B4b；修正 B4a 用 preload 常量、B4b 读 `base_camp_bag_ui.gd` 文案区分

**影响文件：**
- `scripts/ui/base_camp_bag_ui.gd`（新建）
- `scripts/ui/base_ui.gd`
- `tools/mia_phase1_probe.gd`

**测试步骤：**
1. headless：`godot --headless --import --quit-after` 后 `MiaPhase1Probe.tscn`（B4a/B4b PASS，全 **65 PASS**）
2. F5 大营：右窗底部见大营背包网格 +「管理装备」；Dock「背包」仍开完整装备 UI

**验收标准：**
- [x] B4a 网格宿主 + 空背包底格 ≥4
- [x] B4b 接入 CampBagScroll、无 BagPlaceholder、与 RunGrid「安全箱」文案区分
- [ ] F5 右窗背包网格肉眼验收

**是否允许进入下一任务：** YES（探针齐；F5 手测可并行 CTO 验收）

---

### [T-MARCH-M1 / T-MARCH-V1] 自动搜索 + 飘字

**完成内容：**
- `MarchSearchService`：每 Δm 检定 `march_search_pools.json`；接战/采集时 `RunDriver` 不 tick
- `MarchSearchToast` + `RunEventPresenter` `march_search_hit`；`grassland` / `test_01` 地图配置
- `RunMarchLane` 扩展：`GATHER_BEAT`、`MarchEventMarkers` / `MarchGatherView` 骨架（V2/V3 待 M2）
- 文档：`design-march-visual.md` §十二；`design-march-events.md` M1 ✅

**影响文件：**
- `scripts/run/march_search_service.gd`（新建）
- `scripts/ui/march_search_toast.gd`、`march_event_markers.gd`、`march_gather_view.gd`（新建）
- `data/march_search_pools.json`（新建）
- `scripts/ui/run_march_lane.gd`、`scripts/run/run_driver.gd`、`scripts/run/run_event_presenter.gd`
- `scripts/core/data_loader.gd`、`scripts/run/world_run.gd`、`data/map_templates.json`

**测试步骤：**
1. F5 `test_01`：行军约每 10m 底栏【搜索】飘字 + 顶栏 hint；接战期间停止
2. 待补 headless 探针（M1a：接战不触发搜索）

**验收标准：**
- [x] M1 逻辑 + 双通道文案
- [x] V1 Toast 叠在 RunMarchLane
- [ ] F5 test_01 肉眼
- [ ] headless M1a 探针登记

**是否允许进入下一任务：** YES → **T-MARCH-M2**

---

### [CTO] PROJECT_STATUS 同步（开发交接 2026-06-06）

**板面摘要：**
- **探针总量** → **65 PASS**（含 B3/B4）
- **T-UI-B1～B4** → ✅ 逻辑 CTO YES（F5 肉眼并行）
- **T-RUN-V1～V5** → ✅ CTO YES（F5 并行）
- **T-REFACTOR M1～M3** → ✅ CTO YES
- **T-MARCH-M1/V1** → ✅ 已交付（待探针/F5）
- **当前开发指针** → **`T-MARCH-M2`**（里程碑事件）
- **运维** → 本地大批量未 push 改动，开发接手先分批 commit

**给 Dev 的一句话：** 环 1 骨架已齐；先 **M2 里程碑事件**，半日 **探针日** 收 F5；美术走 **T-ART-FW**，不碰战斗数值。

---

### [T-MARCH-M2～M3 / V2～V3] 跑图搜索与事件 · 收官

**完成内容：**
- **M2** `MarchEventService` + `march_events.json` + 地图 `march_events[]`
- **V2** `MarchEventMarkers` 接地图锚点；返程/接战隐藏
- **V3** loot 里程碑 → `GATHER_BEAT` + `MarchGatherView`；延迟结算
- **M3** `retreat_pool_id` + 低稳定加权 + 盾破禁正面物资搜索

**headless：** `MiaPhase1Probe.tscn` → **77 PASS / 0 FAIL**（含 M2a/b、MV2a/b、MV3a～d、M3a～d）

**是否允许进入下一任务：** YES → **T-ART-FW-1**（并行 **探针日** F5）

---

### [CTO] 板面同步 · 探针日（2026-06-06 续）

**开发指针** → **`T-ART-FW-1`**（视觉常量 + `VisualSlot`）

**探针日 F5 清单（不占指针 · 建议半日）：**

| 包 | 操作 | 通过标准 |
|----|------|----------|
| 环 1 | F5 `test_03` | M1 探针 1～5；`user://run_probe.log` 有 ENCOUNTER/DISTANCE/CHASE 行 |
| 视差 | F5 `test_01` / `test_03` | 进军/返程视差；接战停滚；追击剪影 |
| 大营 | F5 大营 | B1.5 点选出征；B2 顶栏稳定；B3 编组卡；B4 背包网格 |
| MARCH | F5 `test_01` + `grassland` | 【搜索】10m；80m 采集事件；返程搜索偏险 |
| MIA | F5 `test_09` 等 | 回收/压力/救援（逻辑已齐） |

**运维：** `main` 超前 `origin` 4 commit（V2/V3/M3）→ push 后远端对齐。

**日常冒烟：** `test_03` + `test_01`（不必每日全表 MiaPhase1Probe）。

---

### [T-ART-FW-1] 视觉常量 + VisualSlot

**完成内容：**
- `VisualConstants`：行军队列/里程碑/视差/采集/Boss 追击占位色与尺寸；`placeholder_spec(art_key)`
- `VisualSlot`：`apply_art_key` / `apply_placeholder` / `apply_texture` / `clear_slot`
- 接线示范：`RunMarchView`、`MarchEventMarkers`、`ParallaxBackdrop` 改读常量表

**headless：** FW1a/b → **79 PASS**

**是否允许进入下一任务：** YES → **T-ART-FW-2**

---

### [T-ART-FW-2] 表现层挂 VisualSlot

**完成内容：**
- `RunMarchView` / `MarchEventMarkers` / `MarchGatherView` / `BossChaseSilhouette` / `ParallaxBackdrop` 子节点全部改 `VisualSlot`
- `VisualSlot` 增补 `set_placeholder_color` / `resize_placeholder`
- Boss 追击危险色迁入 `VisualConstants`

**headless：** FW2a～d → **83 PASS**（V2/MV2 回归仍 PASS）

**是否允许进入下一任务：** YES → **探针日 F5**（并行）或 **T-ART-FW-3**（可选）

---

## 收工（2026-06-06 · 用户暂离）

### 元信息

| 项 | 值 |
|----|-----|
| 阶段 | 环 1 ✅ · MARCH M1～M3 ✅ · ART-FW-1/2 ✅ · headless **83 PASS** |
| 开发指针 | **T-ART-FW-3**（可选）或 **探针日 F5**（并行） |
| 起始 commit | `e5116aa`（ignore probe scratch） |
| 结束 commit | `7d98817`（收工 worklog）；当日共 push **10** commit 至 origin |
| Godot | 4.6.3.stable（已关 12 个后台实例） |

### 今日完成（摘要）

| 轨道 | 结论 |
|------|------|
| **T-REFACTOR M1～M3** | ✅ CTO YES — 编排可测、SaveSerializer、WorldRun 调度、legacy UI 移除 |
| **T-RUN-V1～V5** | ✅ 逻辑 YES — 视差/接战/返程/Boss 剪影 |
| **T-UI-B1～B4** | ✅ 逻辑 YES — F5 肉眼待你/QA 并行收 |
| **T-MARCH M1～M3 + V1～V3** | ✅ 搜索/里程碑/采集/返程池；headless 77→83 PASS |
| **T-ART-FW-1/2** | ✅ VisualConstants + VisualSlot 接线 |
| **T-MIA-0D** | ✅ 文档 `e7f40b6`（更早合入） |
| **CTO 板面** | 对齐表写入 `PROJECT_STATUS`；指针曾切 M2→现 **ART-FW 后 / 探针日** |

### 收工前补丁

- `RunMarchLane` 子节点改 **延迟 load**（避免编辑器 preload 链编译失败）
- MARCH / 视差 / VisualSlot 小修（10 个 `.gd`）

### 测试 / 验收

- [x] headless MiaPhase1Probe **83 PASS**
- [ ] 探针日 F5（环 1 / 视差 / 大营 / MARCH / MIA）— **下次第一步**
- [x] 关闭全部 Godot 后台（11×4.6.3 + 1×4.2.2 编辑器）

### 未决 / 风险

- `origin/main` 落后本地多 commit → **本次收工 push**
- F5 肉眼与逻辑 YES 仍分离；不挡下次 ART-FW-3 或产品迭代

### 下次第一步（你晚点回来）

1. `git pull` 确认远端已对齐  
2. 半日 **探针日**：`test_03` + `test_01` + 大营 B 线肉眼（见上文 F5 清单）  
3. 可选开 **T-ART-FW-3** 或继续美术对接（程序不挡）

### 对话要点

- 新开发交接包已发；环 1 后可叠 MIA/MARCH  
- CTO 五维对齐：骨架 ✅ / B 线 F5 并行 / 跑图下一刀里程碑已做 / 美术等 T-ART-FW / git 首日整理  
- 用户收工：写日志 + 上传 git

---

## 探针日收盘（2026-06-06 · 用户 F5）

### 用户 F5 结论

- **test_01**：进军接战正常；顶栏「接战中」；距离停滚；战斗日志/技能正常  
- **MARCH**：行程提示有搜索/事件；**接战期间左侧不再刷【搜索】** — 符合设计  
- **大营壳**：出征网格（安全箱/外露）、底栏 Dock 随流程可见 — 无异常  
- **总体**：用户确认「没什么问题」

### CTO 登记

| 包 | 结论 |
|----|------|
| 探针日·冒烟 | ✅ YES |
| T-ART-FW-3 | ✅ YES（headless FW3a/b · 85 PASS） |
| 延期 F5 | test_03 追击 · grassland 80m · MIA test_09 · T-02a test_06 |

**下一项**：待 CTO 指派；建议 F5 补测或 T-02a / T-06 产品 TASK。

---

### [T-02a] 濒死返程站位 · F5 YES（用户 test_06）

**F5 观察：**
- ⑥·前排铁卫先濒死；个人压力过低后 **强制退场**（P3，非 T-02a 缺陷）
- 敌人 **主要打术士**，非持续打铁卫 ✅
- 铁卫濒死期间 **不攻击、不自主移动**（至压力退场）✅

**headless：** 02a-1～3 · **88 PASS**

**是否允许进入下一任务：** YES → **T-01**

---

## Dev Sprint · B 线第 1 周收工（2026-06-06）

### 元信息

| 项 | 值 |
|----|-----|
| Sprint | Dev Sprint 2026-06-06 起 · 第 1 周 B 线 |
| 项目路径 | `C:\Users\19173\Desktop\TBH_Idle_RPG` |
| headless | **117 PASS**（0 FAIL） |
| 玩法代码 | 仅 `run_driver.gd`（M2c）；其余为文档 + JSON |

### B 线交付

| ID | 完成内容 | 探针 / 文件 |
|----|----------|-------------|
| **B-1** | 接战/返程战禁用 `march_search` + 里程碑 | M2c · `run_driver.gd` |
| **B-2** | `UI_SUBSYSTEM_AUDIT.md` 同步 T-01/T-03/T-06/T-04/T-02c | 文档 only |
| **B-3** | 草原里程碑 80/120/165/200m + 3 事件定义 | B3-1/B3-2 · `march_events.json` · `map_templates.json` |

### 战斗专题逻辑（本 Sprint 前序 · headless 已 YES）

| ID | 状态 | F5 |
|----|------|-----|
| T-01 套装 | 逻辑 YES | ⏸ 延期 |
| T-02c 纯佣兵 | 逻辑 YES | ⏸ 延期 |
| T-03 技能 CD | 逻辑 YES | ⏸ 延期 |
| T-04 测试模式 | 逻辑 YES | ⏸ 延期 |
| T-06 Buff/觉醒 | CTO YES | ✅ test_08 |

### A 线 F5 结论（待登记 · D1～D5）

| 批次 | 项 | worklog |
|------|-----|---------|
| D1 | T-02c + T-01 套装 | [x] |
| D2 | T-04 + T-03 | [ ] |
| D3 | test_03 环 1 + `run_probe.log` | [ ] |
| D4 | grassland 80m/120m 里程碑采集 | [ ] |
| D5 | test_09 MIA 冒烟 | [ ] |

> A 线测完请在本表改 `[x]` 并回复 `T-0X YES` / `允许`；CTO 写入 `PROJECT_STATUS` §当前任务。

### 测试 / 验收

- [x] `MiaPhase1Probe` **117 PASS**
- [x] M2a 仍含 grassland **80m** `abandoned_crate` 锚点
- [x] A 线 F5 **D1**（截图：铁卫 3/3 + 战略核心留营 + 半组 A 可出战）
- [ ] A 线 F5 D2～D5
- [ ] `git` 分批 push（D1 余项 · 待用户）

### 未决 / 风险

- F5 与逻辑 YES 仍分离；**不挡** B 线收工
- `rusted_cart` 已挂 grassland 240m；`trail_ambush_sign` 已挂 test_09
- 环 1 `test_03` 五探针未 F5 YES 前不排新战斗机制

### 续工收工（T-MARCH-C1 + T-02b · 2026-06-06）

- **T-MARCH-C1 ✅**：test_03～09 + forest/cave/death_trial 里程碑；探针 **C1-3** PASS
- **T-02b ✅**：`BattlefieldSlots` 60×48 脚线 · `CombatView` 脚线随 lane 拉伸 · 探针 **02b-1/02b-2** PASS
- headless：**122 PASS**（0 FAIL）

### A 线 F5 开工（2026-06-06）

- 已执行：`python tools/build_test_save.py` → 槽位 1 测试档（全图解锁 + MIA fixture）
- Godot **F5** → 角色创建 → **继续游戏（槽位 1）**
- 右屏对照：`docs/TEST_PLAYBOOK.md` + 下表 D1～D5
- 测完回复：`D1 YES` … 或贴异常（地图/步骤/现象）

### 下次第一步（B 线 · 第 2 周或 A 线空档）

1. **A 线**：登记 D1～D5 勾选 → CTO 写 `PROJECT_STATUS`  
2. B 线：**T-ART-C1** 挂真图（美术）· 或 **design-meta-base** 局外成长初稿  
3. 合码前再跑：`godot --headless --scene res://tools/MiaPhase1Probe.tscn` → **122 PASS**

---

## 收工（用户暂离 · 2026-06-06 夜）

### 元信息

| 项 | 值 |
|----|-----|
| 起始 commit | `9bc08d3`（T-02a） |
| 结束 commit | `6f8ff7f`（已 push origin/main） |
| headless | **122 PASS**（续工 T-MARCH-C1 + T-02b 后） |

### 今日摘要（会话 + 工作区）

| 轨道 | 结论 |
|------|------|
| **探针日** | test_01 F5 冒烟 YES；接战期间不刷搜索 |
| **T-ART-FW-3** | manifest + VisualSlot · 85→合入 |
| **T-02a** | F5 test_06 YES；02a headless；敌人优先打活人 |
| **T-MARCH-C1 / T-02b** | 里程碑扩图 + 战场 60×48 脚线 · 探针 PASS |
| **B 线 W1** | B-1 M2c · B-2 审计 · B-3 草原里程碑 |
| **战斗包** | T-01/T-02c/T-03/T-04 headless YES（工作区大 diff） |
| **T-01 开工包** | 已发开发；逻辑层多已接线，待 01 探针 + F5 |

### 未决（睡醒后继续）

- **A/B 半组 + 备战 UI** 仍有体验/语义问题（`formation_ui` / `squad_ui` / 编组池点击）— 对齐 **T-UI-FORM-1～4**，**T-UI-FORM-F5** 未签字  
- A 线 F5 **D2～D5** 未勾完  
- 本地 `tools/t*_probe.txt` 等为探针草稿，**不入库**

### 测试 / 验收

- [x] MiaPhase1Probe **122 PASS**（合码前建议再跑一遍）
- [x] T-02a F5（test_06）
- [x] 探针日 test_01 冒烟
- [ ] 备战 UI / AB 半组 F5（用户反馈仍有问题）
- [ ] D2～D5 F5 批次

### 下次第一步

1. `git pull` → F5 复现 **AB 组 / 备战 UI** 问题（截图+步骤）  
2. 开 **T-UI-FORM-1** 或继续热修 `formation_ui.gd`（单 TASK）  
3. 并行：T-01 F5 套装穿脱（后勤 2/3 件铁卫）

### 对话要点

- 用户收工睡觉；要求 **push + 工作日志**  
- AB 组与备战 UI 明确 **未收口**，不冒充 YES

---

## 2026-06-07 追加 · T-UI-CAMP 产品定案

| 项 | 内容 |
|----|------|
| 定案 | **CQ 大营营地舞台观感 + THB 三窗/Dock 交互**（用户提案 CTO YES） |
| 修订 | `GAME_BIBLE` §五、`design-pc-shell`、`design-art-checklist`、`design-base-ui` §T-UI-CAMP、`PROJECT_STATUS` §T-UI-CAMP |
| 不做 | 全屏 CQ 街景横滑；删 Dock；`BaseUI` 长滚复活 |
| 任务线 | CAMP-1～4；**门禁**：FORM-3R CTO YES 后再开 CAMP-1 |
| 程序指针 | D 线仍 **FORM-3R 优先**；E 线 CAMP-1 排队 |

---

## 2026-06-08 追加 · T-UI-STAGE 底栏动画舞台 P0

| 项 | 内容 |
|----|------|
| 用户反馈 | 挂机游戏须在屏幕下方有战斗/休息/养伤 **动画**（CQ）；现底栏仅 StandbyLabel 不达标 |
| 现网 | `camp_stage.gd` 在中窗有占位；`main_shell` BASE 底栏无动画 |
| 定案 | **T-UI-STAGE-1～4** P0；CAMP-1 降为缩略预览；CAMP-3 并入 STAGE-2 |
| 下一开发 | **T-UI-STAGE-1**（BottomStage 状态机）→ STAGE-2（营火 idle） |

---

## 2026-06-08 追加 · T-UI-TWIN-1 双窗定案入任务板

| 项 | 内容 |
|----|------|
| 用户定案 | 动画场景与计划 UI **两个 OS 窗口** |
| 现网 | `main.gd` + `stage_shell.gd` + `stage_window.tscn`；`MainShell` 仅 UpperArea+Dock |
| 任务板 | `PROJECT_STATUS` §T-UI-TWIN；STAGE-1 并入副窗；下一单 STAGE-2 真图 |
| CTO | TWIN-1 / STAGE-1 **待 F5 验收** |

---

## 2026-06-09 追加 · T-UI-LAYOUT 用户分区定案

| 上窗 TBH | 下窗 CQ |
|----------|---------|
| 选图/策略（左+Dock）、背包/装备（右）、结算摘要 | 营火 idle、后勤建筑、养伤动画、行军/接战、抵营动画 |
| 编组 **方案 B**：下窗点人 + 上窗简表 → FORM-LAYOUT-1/2 | |
| **不做** | 街景横滑、上窗 F5 后勤大弹窗为主入口、编组/背包塞下窗（编组未决前） |
| 新 TASK | **T-UI-STAGE-5** 下窗后勤；废止 CAMP-2 上窗后勤 Tab |

---

## 2026-06-09 追加 · HudDock 右下定案

| 项 | 内容 |
|----|------|
| 定案 | `HudDock` **右下**，贴 `StageBand` 上沿（对齐 CQ 截图） |

---

## 2026-06-09 追加 · T-UI-WORLD-REEL 入任务板

| 项 | 内容 |
|----|------|
| 定案全文 | [design-world-reel-CTO.md](../design-world-reel-CTO.md) |
| E 线指针 | **REEL-1** 下一开发；废止 **SHELL-3** |
| 并行 | **FORM-3R**、**SHELL-2** 与 REEL-1 并行；**SHELL-1** 吸收进 REEL-1 |
| 接战 | **REEL-3** CombatSlice 块内；非 60/40 右栏 |
| 资源条 | 角标左侧横向紧凑 |
| 任务 | T-UI-CQ-SHELL-2 验收写入 |
