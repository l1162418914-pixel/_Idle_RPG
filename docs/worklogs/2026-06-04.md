# 工作日志 2026-06-04

## 元信息

| 项 | 值 |
|----|-----|
| 机器 | B（环境就绪）；A 推送收尾 |
| 项目路径 | B：`C:\Users\l1162\Desktop\TBH_Idle_RPG` |
| 结束 commit | `14b720d` fix: main.gd multiline percent format; add B-machine sync doc |
| Godot | 4.6.3.stable |

## 今日完成

- B 机 `git pull`：`a643ddc` → `14b720d`，工作区干净。
- 格式检查 exit 0；`--import` / headless 无 ERROR。
- B 机 **F5 smoke test 通过**（能进 CharacterCreate）。
- A 机推送：`main.gd` 多行 `%` 修复；新增 `docs/SYNC_B_MACHINE.md`。
- 功能批次在 `a643ddc`：测试图 8 张、编队 UI、撤离物、Boss 追击/深度反击、濒死/觉醒、`RunExtractItem` 等。

## 测试 / 验收

- [x] F5 能进 CharacterCreate（B）
- [x] 格式检查 exit 0
- [ ] 按 `docs/TEST_SCENARIOS.md` ①→⑦ 系统验收（待后续会话）

## 未决 / 风险

- 两个月主要在 **B 开发**；勿再依赖「仅 A push」旧说法。
- 玩法验收尚未按测试手册跑完。

## 下次第一步

1. `git pull`
2. 读上一篇 worklog
3. 大营编队 UI 或 **测试①** `retreat_drill`（见 `TEST_SCENARIOS.md`）

## 对话要点

- 双机改为 **B 写 + 测 + push**；GitHub `main` 为唯一真相源。
- 易错：`docs/GDSCRIPT_SYNTAX.md`（多行 `%`）；全局类自引用用 `preload`。
- 收工习惯：代码 + `docs/worklogs/YYYY-MM-DD.md` 一起 commit/push。
