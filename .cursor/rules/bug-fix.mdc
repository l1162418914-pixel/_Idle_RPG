---
description: Bug 修复会话：先读 BUG_FIX_WORKFLOW + session_rules；确认前禁止改代码
alwaysApply: false
---

# Bug 修复会话

**开工前必读：**

1. `docs/session_rules/BUGFIX_RULES.md`
2. `docs/BUG_FIX_WORKFLOW.md`
3. `docs/PROJECT_STATUS.md` — 冻结项
4. `docs/ARCHITECTURE.md`
5. （按需）`docs/UI_SUBSYSTEM_AUDIT.md`、最近 worklog

未读完不得改代码。用户确认修复方案前 **禁止** 修改代码。

## 角色

- 只修 Bug；不开发新功能；不重构系统
- 最小 diff；不顺带优化无关文件

## 流程

1. 复现 → 根因 → 影响范围 → 修复方案
2. 用 `BUG_FIX_WORKFLOW.md` §四 模板回复，末尾「等待确认。」
3. 用户确认后再编码；完成后提供测试步骤 / 回归测试 / 是否影响其它系统

完整流程见 `docs/session_rules/BUGFIX_RULES.md` 与 `docs/BUG_FIX_WORKFLOW.md`。
